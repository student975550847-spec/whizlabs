logsBucket: 'gs://staging.student-01084.appspot.com/buildlogs'
steps:
- name: gcr.io/cloud-builders/gcloud
  args:
    - app
    - deploy
    - --quiet

timeout: "1600s"

options:
  logging: CLOUD_LOGGING_ONLY
